A team and I used SeleniumBasic to automate an email list for a client. It takes the login information as well as a Time Based One Time Pasword TOTP (when appliciable), logs in as the client, grabs the needed emails, and then blasts out emails based on a template to each individual email. The credentials are only kept in RAM so all credentials are safely removed from the system by the time it terminates.
